// SQLite 사용 중단 (Supabase로 이전)
export const initializeDatabase = () => {
  console.log('💡 SQLite 초기화 건너뜀 (Supabase 사용 중)');
};

export const closeDatabase = () => {
  console.log('💡 SQLite 종료 건너뜀 (Supabase 사용 중)');
};

export default null;
